let help = (v) => {
  return `
╭﹝🄼🄰🄸🄽﹞
├ ${v}menu
╰────────
╭﹝🄸🄽🄵🄾🅁🄼🄰🅃🄸🄾🄽﹞
├ ${v}infogempa
├ ${v}wiki (query)
├ ${v}wikien (query)
╰────────`
}

module.exports = { help }
